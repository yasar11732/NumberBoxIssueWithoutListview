<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NumberBoxIssueWithoutListview.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NumberBoxIssueWithoutListview"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="36,72,36,*" RowSpacing="12">
        <Button Command="{x:Bind ViewModel.ShiftCommand}" Content="Change Data Context" />
        
        <NumberBox
            Grid.Row="1"
            Header="Numberbox Under Test"
            Value="{x:Bind ViewModel.Selected.Value, Mode=TwoWay}"
            Minimum="{x:Bind ViewModel.Selected.Min, Mode=OneWay}" />
        
        <TextBlock FontSize="22" FontWeight="Bold" Grid.Row="2">Current Values</TextBlock>
        
        <ListView ItemsSource="{x:Bind ViewModel.Items}" Grid.Row="3">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Item">
                    <TextBlock Text="{x:Bind Value, Mode=OneWay}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
